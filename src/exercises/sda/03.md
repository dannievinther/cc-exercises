---
title: View Progress Timeline
id: sda-3
video: /assets/vid/sda/sda-ex-1.webm
markers: false
help:
  {
    link: https://developer.mozilla.org/en-US/docs/Web/CSS/animation-timeline/view,
    topic: view(),
  }
startingCSS: |
  .box {
    animation: spin linear both;
    animation-timeline: view();
    animation-range: entry 100% contain 50%;
    width: fit-content;
    place-self: center;
  }
  @keyframes spin {
    to {
      rotate: 180deg;
    }
  }
startingHTML: |
  <div class="box">Rotate me</div>
hints:
  - { type: property, name: animation-timeline }
  - { type: value, name: "view()" }
  - { type: property, name: animation-range }
---

Brug `animation-timeline: view()` til at knytte animationen til elementets synlighed i scrollporten. `view()` er en genvej, der automatisk tracker elementet selv — du behøver ikke oprette en named timeline.
